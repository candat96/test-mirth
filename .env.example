# Mirth Connect Environment Variables
# Copy this file to .env and update values

# Keystore passwords (also defined in secrets/mirth.properties)
KEYSTORE_STOREPASS=changeme_storepass
KEYSTORE_KEYPASS=changeme_keypass

# Session store (set to true for clustered environments)
SESSION_STORE=false
